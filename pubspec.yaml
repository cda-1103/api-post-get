name: excel_p
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  excel: ^2.1.0  # O la última versión estable
  http: ^1.1.0   # O la última versión estable


dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
